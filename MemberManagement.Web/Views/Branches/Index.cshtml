@model IEnumerable<MemberManagement.Application.DTOs.BranchDto>

@{
    ViewData["Title"] = "Branches List";
}

@if (TempData["SuccessMessage"] != null)
{
    <div id="successAlert" class="alert alert-success text-center operation-alert">
        <i class="fa-solid fa-circle-check"></i>
        @TempData["SuccessMessage"]
    </div>
}

@if (TempData["ErrorMessage"] != null)
{
    <div id="errorAlert" class="alert alert-danger text-center operation-alert">
        <i class="fa-solid fa-triangle-exclamation"></i>
        @TempData["ErrorMessage"]
    </div>
}

@section Styles {
    <link rel="stylesheet" href="~/css/branchpg/branchindex.css" />
}

@section Scripts {
    <script src="~/js/branchpg/branchindex.js"></script>
}

<div class="card shadow-lg">
    <div class="card-body">

        @* TOP BAR - Updated with Address and Branch Code inputs *@
        <div class="branch-top-bar mb-3">
            <div class="d-flex justify-content-between align-items-center">
                <div class="d-flex align-items-center gap-2">
                    <div class="btn btn-primary btn-sm btn-branch-icon" title="Branches">
                        <i class="bi bi-diagram-3"></i>
                    </div>
                    <h2 class="mb-0">BRANCHES LIST</h2>
                </div>

                <form asp-action="Create" method="post" class="d-flex align-items-center gap-2">
                    <input type="text" name="branchCode" class="form-control form-control-sm" style="width: 100px;" placeholder="Code" required>
                    <input type="text" name="name" class="form-control form-control-sm" placeholder="Branch Name" required>
                    <input type="text" name="address" class="form-control form-control-sm" placeholder="Address/Location" required>

                    <button type="submit" class="btn btn-sm btn-primary" title="Add New Branch">
                        <i class="bi bi-plus-lg"></i>
                    </button>
                    <a href="@Url.Action("Index")" class="btn btn-sm btn-outline-secondary" title="Refresh List">
                        <i class="bi bi-arrow-clockwise"></i>
                    </a>
                </form>
            </div>
        </div>

        @* TABLE - Added Code and Address Columns *@
        <div class="table-responsive scrollable-table">
            <table class="table table-bordered table-striped branch-table">
                <thead class="table-primary">
                    <tr>
                        <th style="width: 100px;">Code</th>
                        <th>Branch Name</th>
                        <th>Address/Location</th>
                        <th class="text-center" style="width: 120px;">Status</th>
                        <th class="text-center" style="width: 120px;">Manage</th>
                    </tr>
                </thead>
                <tbody>
                    @if (!Model.Any())
                    {
                        <tr>
                            <td colspan="5" class="text-center text-muted py-5">
                                <i class="bi bi-emoji-frown d-block mb-2" style="font-size: 2rem;"></i>
                                No branches found.
                            </td>
                        </tr>
                    }
                    @foreach (var b in Model)
                    {
                        <tr>
                            <td><code>@b.BranchCode</code></td>
                            <td><strong>@b.Name</strong></td>
                            <td>@b.Address</td>
                            <td class="text-center">
                                <span class="badge @(b.IsActive ? "bg-success" : "bg-danger")">
                                    @(b.IsActive ? "Active" : "Inactive")
                                </span>
                            </td>
                            <td class="text-center">
                                <div class="btn-group">
                                    @* EDIT BUTTON *@
                                    <button type="button" class="btn btn-sm btn-outline-primary"
                                            data-bs-toggle="modal" data-bs-target="#editModal"
                                            data-id="@b.Id" data-name="@b.Name"
                                            data-address="@b.Address" data-code="@b.BranchCode"
                                            title="Edit Branch">
                                        <i class="bi bi-pencil"></i>
                                    </button>

                                    @* DELETE BUTTON *@
                                    <button type="button" class="btn btn-sm btn-outline-secondary btn-delete-hover"
                                            data-bs-toggle="modal" data-bs-target="#deleteModal"
                                            data-id="@b.Id" data-name="@b.Name"
                                            title="Deactivate Branch">
                                        <i class="bi bi-trash"></i>
                                    </button>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>

        <div class="d-flex justify-content-between align-items-center mt-2">
            <div class="branch-info-text">
                Total Branches: <strong>@Model.Count()</strong>
            </div>
        </div>
    </div>
</div>

@* EDIT MODAL *@
<div class="modal fade" id="editModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <form asp-action="Edit" method="post">
                @Html.AntiForgeryToken()
                <input type="hidden" name="id" id="editBranchId" />
                <div class="modal-header">
                    <h5 class="modal-title">Edit Branch</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body">
                    <div class="mb-3">
                        <label class="form-label">Branch Code</label>
                        <input type="text" name="branchCode" id="editBranchCode" class="form-control" required />
                    </div>
                    <div class="mb-3">
                        <label class="form-label">Branch Name</label>
                        <input type="text" name="name" id="editBranchName" class="form-control" required />
                    </div>
                    <div class="mb-3">
                        <label class="form-label">Address / Location</label>
                        <textarea name="address" id="editBranchAddress" class="form-control" rows="2" required></textarea>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="submit" class="btn btn-primary">Save Changes</button>
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                </div>
            </form>
        </div>
    </div>
</div>

@* DELETE MODAL *@
<div class="modal fade" id="deleteModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <form id="deleteForm" asp-action="Delete" method="post">
                @Html.AntiForgeryToken()
                <input type="hidden" name="id" id="deleteBranchId" />
                <div class="modal-header">
                    <h5 class="modal-title">Deactivate Branch</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body">
                    Are you sure you want to mark <strong id="branchNameLabel"></strong> as inactive?
                </div>
                <div class="modal-footer">
                    <button type="submit" class="btn btn-danger">Confirm</button>
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                </div>
            </form>
        </div>
    </div>
</div>